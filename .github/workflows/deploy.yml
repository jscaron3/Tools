name: Deploy Tools
on:
  push:
    branches: [main]
permissions:
  contents: write
  pages: write
  id-token: write
concurrency:
  group: pages-${{ github.job }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[skip ci]')"
    steps:
      - uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node build-links.js
      - name: Commit updated index.html
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html tools.json
          if ! git diff --staged --quiet; then
            git commit -m "chore: update index.html [skip ci]"
            git push
          fi
      - name: Prepare Pages artifact
        run: |
          mkdir -p _site
          rsync -a --delete \
            --exclude '.git/' \
            --exclude '.github/' \
            ./ _site/
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4





# name: Deploy Tools

# on:
#   push:
#     branches: [main]

# permissions:
#   contents: write
#   pages: write
#   id-token: write

# concurrency:
#   group: pages
#   cancel-in-progress: true

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - name: Setup Pages
#         uses: actions/configure-pages@v5

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - run: node build-links.js

#       - name: Commit updated index.html
#         run: |
#           git config user.name "github-actions[bot]"
#           git config user.email "github-actions[bot]@users.noreply.github.com"
#           git add index.html tools.json
#           if ! git diff --staged --quiet; then
#             git commit -m "chore: update index.html [skip ci]"
#             git push
#           fi

#       - name: Prepare Pages artifact
#         run: |
#           mkdir -p _site
#           rsync -a --delete \
#             --exclude '.git/' \
#             --exclude '.github/' \
#             ./ _site/

#       - name: Upload Pages artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: _site

#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}

#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4










# name: Deploy Tools

# on:
#   push:
#     branches: [main]

# permissions:
#   contents: write

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - run: node build-links.js

#       - name: Deploy to GitHub Pages
#         uses: peaceiris/actions-gh-pages@v4
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_dir: .